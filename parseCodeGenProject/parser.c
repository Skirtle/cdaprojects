#include <stdio.h>
#include <stdlib.h>
#include "parser.h"
#include "scanner.h"

symbol* parse(lexeme *list, int *no_errors, int list_size, int *table_size) {
    symbol *table = (symbol*) calloc(1, sizeof(symbol) + 12);
    int index = 0;



    return table;
}
